package ucf.assignments;

import javafx.collections.ObservableList;

public class ToDoListItemManagement {
    public static ObservableList<ToDoListItem> addItem(String desc, String date, String status, ObservableList<ToDoListItem> theList) {
        //Will take in the incoming values and create an item
        //Will add the item to the ObservableList of items
        return theList;
    }

    public static ObservableList<ToDoListItem> deleteItem(ObservableList<ToDoListItem> toDelete, ObservableList<ToDoListItem> theList) {
        //loop to delete the item that was selected from the list passed in
        return theList;
    }
}
